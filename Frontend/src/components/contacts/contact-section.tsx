// component for contact-section
import { motion } from "motion/react"; // Motion for animations
import { TypeAnimation } from "react-type-animation"; // for typing animations
// The component
const ContactSection = () => {
    // Returning statement
    return(
        <>
        {/* The section for the content of contacts */}
            <motion.section>

            </motion.section>
        </>
    )
}
export default ContactSection;